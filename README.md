# Project Documentation

This project fetches random jokes from the JokeAPI.

## Usage

Run `joke_generator.py` to get a random joke.

## Requirements

Make sure to install the required packages:

```bash
pip install -r requirements.txt
```